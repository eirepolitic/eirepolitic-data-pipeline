# Session log — 2026-09-20-phase5-existing-series-demo

Phase 5 (plan §5/§6.2) self-test: prove the existing-series workflow runs end-to-end through the actual routing table in `director/README.md`, and that a session-tagged dispatch correctly records its run on this session branch (not `main` — see PR #147, which fixed exactly this wiring gap moments before this session branch was created).

Route taken, per `director/README.md`'s task routing table ("Generate this month's X" -> existing-series workflow, plan §6.2): resolve project from `projects.yml` -> resolve period -> dispatch `instagram_factory_render.yml` with `session_id` set -> declarative QA -> review link recorded back here.

`ipi_polling_factory_v1` was chosen over `party_issue_monthly_profile_v2` because it has no monthly content decision attached (cadence: latest, dispatched on-demand) — this keeps the demo honest as a mechanical proof rather than a stand-in for a real editorial choice only Warren can make.

**Result.** Run `35544517116` completed `conclusion: success`, resolved period `2026-09-13` (latest poll), 4/4 slides, all 3 declarative QA checks PASS. Review page: `previews/ipi_polling_factory_v1`. Critically, the run record landed at `director/sessions/2026-09-20-phase5-existing-series-demo/runs/35544517116.json` on **this session branch**, not on `main` — confirming PR #147's fix works as intended.

**What this proves for Phase 5.** The existing-series workflow (plan §6.2) is fully mechanical and now demonstrably closes the loop with session state: routing table -> project resolution -> dispatch -> QA -> review link -> run record, with no manual step and no AWS-credentialed workflow push required (only a dispatch of the already-merged workflow, per §1.11/§1.13). The other three v1 workflows (§6.1 new post, §6.3 modify-from-feedback, §6.4 data/capability work) are documented as concrete procedures in `director/workflows_v1.md`, but §6.1 and §6.3 inherently require a real content or feedback decision from Warren at specific gates — they are validated by design and by the supporting director/*.yml files they read (projects.yml, visuals.yml's feedback_to_knob, data_products.yml), not by a synthetic run standing in for his input. The first real use of either will be their actual end-to-end proof.
