# Validation review improvement plan

## Goal

Make project validation faster to review, less repetitive, and more reliable while preserving a complete audit trail.

## Principles

- Use current real production data first.
- Search historical production data before synthetic fallback or waiver.
- Keep synthetic contract-edge tests separate and non-publishable.
- Show each unique rendered result once in the summary review.
- Preserve every scenario and decision in the audit output and manifests.
- Fail automatically on measurable layout/rendering defects; reserve human review for design and factual judgement.

## Phase 1 — summary and audit review outputs

**Status: completed**

Implemented:

- `validation_contact_sheet.png` is a concise summary sheet
- the cover is shown once from a representative real example
- visual-shape scenarios show the visual slide only
- legacy `minimum`/`maximum` aliases are omitted from the summary
- identical visual outputs are grouped by SHA-256 hash
- one unique render can carry multiple scenario badges
- waived scenarios appear in one compact summary section
- visible metadata is reduced while full details remain in JSON
- `validation_audit_contact_sheet.png` preserves complete scenario evidence

Acceptance met:

- no duplicate rendered chart appears in the summary
- every required scenario is represented by a render group or waiver
- summary and audit manifests record scenario-to-render grouping
- all outputs remain non-publishable

## Phase 2 — cover and title simplification

**Status: completed for party and constituency production projects**

Implemented:

- removed duplicated party/constituency names from cover media
- removed small draft/status/footer-like cover text
- replaced cover copy with large useful metrics
- shortened visual titles to `Top issues: <item>`
- retained detailed meaning in accessibility text and manifests
- standardized party and constituency cover assets to the tall media-slot aspect ratio

Acceptance met:

- no duplicated headline text within one slide
- no unreadably small cover copy
- title remains within configured line and font-size limits

## Phase 3 — historical real-data fallback

**Status: completed**

Implemented:

- adapter-specific historical S3 source discovery
- current production data searched first
- up to 12 recent historical production batches searched second
- batches ordered by S3 object modification time
- batch, item, source-key, and search-stage provenance recorded
- synthetic contract-edge data is not generated by convenience
- waiver occurs only after current and searched historical data fail
- every scenario records the four-stage decision path:
  - current real
  - historical real
  - synthetic contract edge
  - waiver

Live validation evidence:

- eight historical batches loaded
- zero current waivers replaced by the available history
- three scenarios remained waived
- successful run: `30871813098`

Acceptance met:

- every scenario manifest records search stages
- waivers state how many historical batches were checked
- no synthetic scenario is created for convenience

## Phase 4 — automated whitespace and content-utilization checks

**Status: completed**

Implemented:

- occupied-content bounding-box measurement for final slides
- media-slot vertical and area fill checks
- chart plot-area and plot-height checks
- failure on excessive top/bottom whitespace
- failure on aspect-ratio letterboxing beyond declared thresholds
- machine-readable thresholds in the active layout and visual template

Acceptance met:

- the previously observed vertical letterboxing now fails automatically
- thresholds are layout/visual specific and recorded in manifests

## Phase 5 — renderer readability checks

**Status: partially completed**

Implemented:

- minimum category-label font size
- minimum value-label font size
- minimum axis font size
- minimum bar thickness
- maximum wrapped label lines
- value-label right-edge headroom
- duplicate-render detection in the summary review

Still needed before this phase is complete:

- explicit title shrink/wrap telemetry from the post-layout renderer
- direct text bounding-box clipping checks for category labels
- direct rendered-text clipping checks for value labels
- dynamic font-size selection based on actual label complexity rather than fixed validated sizes

Current acceptance:

- undersized configured text, thin bars, excessive wrapping, and crowded value labels fail before human review
- direct pixel/text-bound clipping detection remains outstanding

## Phase 6 — regression tests and documentation

**Status: in progress**

Completed:

- generic-core regression coverage
- party production-project regression coverage
- constituency production-project regression coverage
- historical fallback precedence and waiver coverage
- image-dimension, media-fill, scenario-coverage, and contact-sheet tests
- successful live S3 validation
- summary and audit outputs in validation artifacts
- this implementation plan updated with actual status

Remaining:

- update the canonical `instagram/CONTENT_FACTORY_PLAN.md` with the completed validation architecture
- update party and constituency project decision records
- final handoff summary of remaining Phase 5 limitations

## Delivery status

1. Phase 1 summary/audit redesign — completed
2. Phase 2 party cover cleanup — completed
3. Phase 4 utilization checks — completed
4. Phase 5 renderer checks — partially completed
5. Phase 3 historical fallback — completed
6. Phase 2 constituency/general cover cleanup — completed
7. Phase 6 final regression and documentation — in progress
