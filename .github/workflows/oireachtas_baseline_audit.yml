name: Oireachtas baseline audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/oireachtas_validation_reusable.yml
    with:
      batch_id: scheduled-weekly-33530737534-1
      run_consumers: true
    secrets: inherit
